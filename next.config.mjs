/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'i0.wp.com',
      'images.unsplash.com',
      'cdn.britannica.com',
      'encrypted-tbn0.gstatic.com'
    ],
  },
};

export default nextConfig;
